# 验收与统计检查

## Material Passport

- Origin Skill: experiment-agent
- Origin Mode: validate
- Origin Date: 2026-09-10
- Verification Status: ANALYZED
- Version Label: v100_benchmark1_delivery_v1

判定：经确认范围内的V100兼容实验、原始数据、分析代码和报告已完成。统计解释为CAUTION（单seed、单次、主机资源共享）；不表示GPU独立重复或全新机器安装已验证。

## 数据与代码检查

| 项目 | 结果 |
|---|---|
| 正式测点与请求 | 21/21测点、2184/2184请求成功，各输出128 tokens，ID唯一 |
| 预热隔离 | 84次预热分开保存；测量批次不混入预热请求 |
| 模型与输入 | 输入SHA256、token数一致；模型/分词文件指纹补采并保存 |
| 测量代码 | 三份测量源码与三组environment.json逐字节SHA一致 |
| 运行版本 | 四个关键包三组一致；pip check无依赖错误 |
| 三张显卡 | 同型号V100-SXM2-32GB、32 GiB、功率上限300 W，每组独占一张 |
| 输出token会计 | 所有批次batch_size之和与总输出token相等 |
| 缓存token会计 | computed_prompt = prompt - cached，21点差值均0 |
| host回载会计 | H→D bytes = host_reuse_tokens×36864，21点差值均0 |
| 原始拷贝事件 | 双向事件字节求和与result.json一致 |
| 双计算路径 | SQLite从原始JSON独立计算中位数、复用率、吞吐、传输统计，与Python结果一致，相对容差1e-9 |
| 工作区单元测试 | 32项通过，不使用GPU |
| 交付目录单元测试 | 10项通过，不依赖原工作区src包 |
| GPU资源释放 | 三个服务已结束；最后检查四张卡均0 MiB、0%利用率 |

SQLite内存表直接装载raw请求与事件，不是对已汇总CSV换个SELECT。SQL位于report/queries，装载和核对逻辑为scripts/v100_chart_queries.py。这属于离线数据处理验证，不是新一轮GPU复现。

## 11类统计误读检查（11/11）

| 类型 | 判定与处理 |
|---|---|
| 辛普森悖论 | 按负载分别对照，不合成平均加速比；不同负载方向确实不同 |
| 生态谬误 | 组级中位数不代表每个请求，同时给CDF与p95/p99 |
| Berkson选择偏差 | 固定合成负载不代表真实应用请求分布 |
| Collider偏差 | 不适用；没有基于结果变量筛选或因果回归 |
| 忽略基率 | 复用率分母明确；host_tokens/prompt不叫external hit/query |
| 回归均值 | 无独立重跑，不根据一次极端值宣称稳定改善 |
| 幸存者偏差 | 2184请求均完成，无丢弃样本；正式失败清单为空 |
| 多重搜索效应 | 21点全部报告，无显著性检验，不只挑有利点 |
| 分析路径自由度 | 补点与并行安排有记录；旧smoke和pilot不混入正式结果 |
| 相关不等于因果 | 同规格三卡共享主机且启动时间不同，不将全部差异归因于缓存策略 |
| 反向因果 | 缓存配置在请求前固定，未做应用层方向性回归 |

没有p值、置信区间或跨seed显著结论。请求内ITL相关，不能当作数万个独立实验重复。

## 图表与报告QA

五张PNG已打开逐一检查：标题、中文标签、单位、图例无明显裁切/重叠；柱图零起点，CDF完整样本保留，ITL增加主体放大插图避免长间隔压扁主体。QPS/长度线图为需求指定的离散配置扫描，不做外推。

HTML由规范artifact构建，29个阅读块、9张原生图表、5个数据集，来源指向实际SQL与装载代码；数据结构、来源和自包含打包检查通过。自动浏览器检查受限：默认找不到专用Chromium headless-shell，改用本机Chrome后，静态图提取在约11.2秒超时。因此HTML目前为 **structural_only**，不宣称通过桌面/窄屏、亮/暗模式或来源弹窗的浏览器验收。没有安装浏览器或修改共享报告运行时。Markdown+PNG为已人工查看的静态阅读备份。

文件SHA256与原始结果复算由scripts/verify_delivery.py检查，压缩包经过ZIP逐文件CRC检查。最终封包回执位于交付文件夹旁的“封包回执.json”。

## 报告结构与图表映射

技术报告采用：标题→技术摘要→实验设置/定义→负载方法→缓存/吞吐→QPS/长度→尾延迟→offload→验证限制→后续问题→来源。为避免误读，定义和方法前移到证据图之前；下一步与进一步问题合并在“复现结论与后续问题”。

PNG使用S0灰、S1蓝、S2橙及线型/标记/纹理区分；HTML使用共享原生分类调色板和线型，颜色不是唯一编码。课程第三方研究材料不添加OpenAI品牌标记。其余图表问题、粒度和省略理由见V100图表口径；新增offload_details为两联分组柱，展示7点双向GB和有效GB/s。

尚未验收：独立GPU重跑、跨seed稳健性、卡间轮换、全新机器安装、原量化/BF16配置、请求级三阶段甘特图、纯kernel耗时、设备队列/加载停顿、overlap和SLO goodput。
